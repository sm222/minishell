# this script is use at start up
# all the empty line will be ignore

# line that start with '#' is comment

#	setup color
ms -s C=01
ms -s C=18
ms -s C=27

#	setup dir len in prompt
ms -s DIRL=2
export MS_DIR=$CONPILE_DIR
export SDIR=$CONPILE_DIR/scrips

#make update
ms -u

#base scrip made by anboisve
#@0cculus add security if line start with '|'